Invalid WordPress sites configuration: site names in `laravel_sites` must have matching entry in `vault_laravel_sites`.

Sites without a matching vault entry:
{% for name in laravel_sites.keys() | difference(vault_laravel_sites.keys()) %}
* `{{ name }}`
{% endfor %}

Update `group_vars/{{ env }}/vault.yml` to continue.

Docs: https://roots.io/trellis/docs/wordpress-sites/#passwordssecrets
